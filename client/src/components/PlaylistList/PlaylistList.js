import React from 'react';
import styled from 'styled-components';

const PlayListBlock = styled.div``;

const playList = props => {
  return <PlayListBlock>PlayList Container</PlayListBlock>;
};

export default playList;
